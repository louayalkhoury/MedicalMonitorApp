# مُراقب القراءات الطبية - Medical Monitor App

تطبيق Android لعرض قراءات طبية لحظية من Arduino عبر Bluetooth HC-05.

## 📱 الميزات

- عرض معدل ضربات القلب (BPM)
- عرض نسبة الأكسجين في الدم (SpO2 %)
- عرض درجة حرارة الجسم (°م)
- اتصال تلقائي مع HC-05 عند التشغيل
- واجهة طبية نظيفة واحترافية

## 🔧 المتطلبات

- Android 5.0 (API 21) أو أحدث
- جهاز HC-05 مُزامن مسبقاً مع الهاتف عبر إعدادات Bluetooth
- Arduino يرسل البيانات بالصيغة: `HR:75,SpO2:98,Temp:36.7`

## 🚀 البناء والتشغيل

1. افتح المشروع في **Android Studio** (File > Open > اختر مجلد MedicalMonitor)
2. انتظر انتهاء Gradle Sync
3. اتصل بهاتف Android عبر USB (مع تفعيل USB Debugging)
4. Build > Build Bundle(s) / APK(s) > Build APK(s)
5. ثبّت الـ APK على الهاتف أو شغّله مباشرة من Android Studio

الملف الناتج: `app/build/outputs/apk/debug/app-debug.apk`

## 📡 آلية الاتصال (مبسطة)

### 1. البروتوكول
- **Bluetooth Classic SPP** (Serial Port Profile)
- **UUID**: `00001101-0000-1000-8000-00805F9B34FB`
- **سرعة**: 9600 baud (من جانب Arduino)

### 2. سير العمل
1. عند فتح التطبيق، يبحث عن الأجهزة المُزامنة (Bonded)
2. يختار الجهاز المُسجّل باسم **HC-05**
3. ينشئ قناة Serial عبر `createRfcommSocketToServiceRecord(SPP_UUID)`
4. يتصل ويبدأ قراءة البيانات من الـ InputStream
5. ي parse السطر النصي `HR:75,SpO2:98,Temp:36.7` ويحدّث الواجهة

### 3. خطوات ما قبل الاستخدام
1. تفعيل Bluetooth على الهاتف
2. من إعدادات Bluetooth: ابحث عن الأجهزة وجد **HC-05**
3. اضغط عليه للمزامنة (Pairing) - أدخل PIN إن طُلِب (عادة 1234)
4. بعد المزامنة، شغّل التطبيق - سيتصل تلقائياً

### 4. تغيير اسم الجهاز أو استخدام MAC
إذا كان اسم HC-05 مختلفاً، عدّل في `MainActivity.kt`:
```kotlin
private val HC05_DEVICE_NAME = "اسم_جهازك"
// أو استخدم MAC مباشرة:
private var hc05Mac = "00:11:22:33:44:55"
```

## 📂 هيكل المشروع

```
MedicalMonitor/
├── app/
│   ├── src/main/
│   │   ├── java/com/medicalmonitor/app/
│   │   │   └── MainActivity.kt
│   │   ├── res/
│   │   │   ├── layout/activity_main.xml
│   │   │   ├── values/colors.xml, strings.xml, themes.xml
│   │   │   └── drawable/
│   │   └── AndroidManifest.xml
│   └── build.gradle.kts
├── build.gradle.kts
└── settings.gradle.kts
```

## ⚠️ ملاحظات

- **Android 12+**: قد يطلب النظام إذن Bluetooth عند التشغيل الأول (حسب إعدادات الجهاز).
- التطبيق لا يقوم بالبحث عن أجهزة جديدة - فقط يتصل بالأجهزة المُزامنة مسبقاً.
- تأكد أن Arduino يرسل البيانات بصيغة صحيحة وبسرعة 9600.
